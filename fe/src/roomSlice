import {createSlice} from "@reduxjs/toolkit";


const slactSlice = createSlice(
    {
      name:"slactSlice",
      initialState:{},        
      reducers:{
        joinData:(state, action) => {          
          state.value = action.payload
        },  
      }
    }
  )

  export default slactSlice;
  