import styled from 'styled-components';
import logo from "../../../assets/slack_logo.svg"

export const LoginHeader = styled.h1`
margin-bottom:45px;
`

export const LogoImage = styled.img.attrs({src: `${logo}`})`
width: 200px;
`